import React, { Component } from 'react'

export default class CountyList extends Component {
    
    render(){
    return (
      <div>
         {/* {props.spot.map(spotdata => {return(<h3>{spotdata.spot_name}</h3>)})} */}
      </div>
    )
  
}
}
